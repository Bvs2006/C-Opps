#include<iostream>
#define SIZE 100
class stack{
private:
    int arr[100],top;
public:
stack()
{
    this->top=-1;
}
void push(int x)
{
    if(!isfull()) arr[++top]=x;
}
void  pop()
{
    if(!isempty())top--;
}
bool isempty()
{
    return top==-1;
}
bool isfull()
{
    return top==SIZE;
}

};
